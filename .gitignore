# Doesn't contain sensitive files
# Contains provider plugins, module cache
# Excluded to avoid bloating the rpo
.terraform/

# Most improtant files to ignore as they contain credeentials by terraform
terraform.tfstate
terraform.tfstate.backup
variables.tf

